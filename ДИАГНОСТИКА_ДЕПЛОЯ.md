# 🔍 Диагностика проблем с автоматическим деплоем

## Возможные причины, почему изменения не загрузились на сайт:

### 1. ❌ Секреты не настроены в GitHub (самая частая причина)

**Как проверить:**
1. Откройте: https://github.com/PeppaKHR/foxford/settings/secrets/actions
2. Проверьте, есть ли там два секрета:
   - `FTP_USERNAME`
   - `FTP_PASSWORD`

**Если секретов нет:**
1. Нажмите "New repository secret"
2. Добавьте:
   - **Имя:** `FTP_USERNAME` → **Значение:** `u3353778`
   - **Имя:** `FTP_PASSWORD` → **Значение:** `Gjkbyf01!_`

### 2. ❌ Workflow не запустился или завершился с ошибкой

**Как проверить:**
1. Откройте: https://github.com/PeppaKHR/foxford/actions
2. Найдите последний запуск workflow "Deploy to FTP"
3. Посмотрите статус:
   - ✅ Зеленый = успешно
   - ❌ Красный = ошибка (нажмите на него, чтобы увидеть детали)
   - ⚠️ Желтый = в процессе

**Если workflow не запустился:**
- Проверьте, что вы делали `git push` в ветку `main`
- Убедитесь, что файл `.github/workflows/deploy.yml` существует в репозитории

### 3. ❌ Ошибка подключения к FTP

**Возможные причины:**
- Неправильный пароль в секретах
- Неправильный формат username (для reg.ru иногда нужен `u3353778@pkhromova.online`)
- FTP сервер недоступен
- Неправильный путь к директории

**Как исправить:**
- Проверьте логи workflow в разделе Actions
- Убедитесь, что пароль правильный: `Gjkbyf01!_`
- Попробуйте изменить формат username в workflow

### 4. ✅ Быстрое решение: запустить деплой вручную

Если секреты настроены, можно запустить деплой вручную:
1. Откройте: https://github.com/PeppaKHR/foxford/actions
2. Выберите workflow "Deploy to FTP"
3. Нажмите "Run workflow" → "Run workflow"

## 📋 Чек-лист для проверки:

- [ ] Секреты `FTP_USERNAME` и `FTP_PASSWORD` настроены в GitHub
- [ ] Workflow запускается при `git push` (проверьте в Actions)
- [ ] Workflow завершается успешно (зеленый статус)
- [ ] Нет ошибок в логах workflow
- [ ] Файлы действительно изменились в репозитории

## 🔧 Альтернативный вариант: использовать SFTP

Если FTP не работает, можно попробовать SFTP:
1. Откройте: https://github.com/PeppaKHR/foxford/actions
2. Выберите workflow "Deploy to SFTP (Alternative)"
3. Нажмите "Run workflow"

